{
  "name": "image-uploader",
  "version": "1.0.0",
  "description": "Image uploader",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node index.js",
    "install": "cd backend && npm install && cd ../frontend && npm install && cd ..",
    "build": "rm -fr backend/build && cd frontend && npm run build && cp -r build ../backend"
  },
  "author": "",
  "license": "ISC"
}